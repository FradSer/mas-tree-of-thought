scopes:
    - name: dialectica
      description: core engine, agents, and processing logic in dialectica/; does not cover evaluation scripts or pattern examples
    - name: evals
      description: benchmark suites and ablation harnesses in evals/; does not cover core engine source code
    - name: tests
      description: automated BDD scenarios and unit test suites in tests/; does not cover application source code
    - name: examples
      description: pattern implementations and usage demonstrations in examples/; does not cover core logic or evaluation harnesses
